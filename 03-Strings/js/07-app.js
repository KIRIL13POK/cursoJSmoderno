'use strict';

const producto = 'Monitor de 20 pulgadas';
//El toUpperCase() método devuelve el valor convertido en mayúsculas de la cadena que realiza la llamada:
console.log(producto.toUpperCase());
//El método toLowerCase devuelve el valor de la cadena convertida a minúsculas:
const correo = 'PEPERONI007@GMAIL.COM';
console.log(correo.toLowerCase());

const precio = 300;
console.log(precio);
console.log(precio.toString());