'use strict';
const producto = {
    nombre : 'Monitor de 20 pulgadas',
    precio : 300,
    disponible : true,
};
 //Accseder nombre:
 console.log(producto.nombre);
 console.log(producto['nombre']);
