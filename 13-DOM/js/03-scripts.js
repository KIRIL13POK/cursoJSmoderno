// Los ID solamente se debe utilizar un ID con ese nombre en el documento, retorna el primero que encuentre
const formulario = document.getElementById('formulario');
console.log( formulario);
// ID no exixste devuele null